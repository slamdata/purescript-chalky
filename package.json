{
  "private": true,
  "scripts": {
    "clean": "rimraf output",
    "build": "pulp build --censor-lib --strict",
    "test": "pulp test"
  },
  "dependencies": {
    "chalk": "^1.1.1"
  },
  "devDependencies": {
    "pulp": "^9.0.1",
    "purescript": "^0.9.1",
    "purescript-psa": "^0.3.9",
    "rimraf": "^2.5.2"
  }
}
